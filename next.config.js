/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "phinf.pstatic.net",
      "k.kakaocdn.net",
      "https://fakestoreapi.com",
    ],
  },
};

module.exports = nextConfig;
